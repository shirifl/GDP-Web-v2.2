[build]
  command = "npm run build"
  publish = "dist"

# The app uses hash-based routing, so deep links resolve client-side.
# This redirect is a safety net for any non-hash path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
